{
  "name": "cookbook-lambda",
  "private": true,
  "scripts": {
    "dev": "nodemon ./node_modules/.bin/serverless offline --stage local",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "serverless": "^2.35.0",
    "serverless-offline": "^6.9.0"
  }
}
